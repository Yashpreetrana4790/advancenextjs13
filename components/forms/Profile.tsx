import React from 'react'

const Profile = ({ userId, userInfo }: any) => {

  // const { name, username, email, picture, bio, portfolioWebsite, location, joinedAt } = JSON.parse(userInfo)
  return (
    <div>Profile</div>
  )
}

export default Profile